/**
 * @typedef {Object} HandlerSettings
 * @property {boolean} enabled
 */

/**
 * @typedef {Object} Handler
 * @property {Function} executeHandler - Execute handler method
 * @property {HandlerSettings} settings
 */

module.exports = {};
